export type perfumeType = {
    name: string,
    pic: string,
    type: string,
    sizes: string[],
    _id: string,
    price: number,
}

export type user = {
    name: string,
    email: string,
    password: string,
}